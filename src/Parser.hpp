#ifndef PARSER_H_
#define PARSER_H_


class Parser {

    public:
        /** Parser functions:
        *  the initial_settings function processes user choices and calls the editing methods
        *  the function_extractor extracts user specified information
        */
        void initial_settings();
        void function_extractor(); 
};

#endif